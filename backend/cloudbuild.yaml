steps:
  # Mengambil data env yang disimpan di cloud storage menggunakan gsutil
  - name: "gcr.io/cloud-builders/gsutil"
    args: ["cp", "gs://tcc-f08-133/.env", ".env"]

  # Membuat image dengan perintah "docker build -t gcr.io/$PROJECT_ID/notes-backend-farros ."
  - name: "gcr.io/cloud-builders/docker"
    args: ["build", "-t", "gcr.io/$PROJECT_ID/notes-backend-farros", "."]

  # Meng-upload/push image yang telah dibuat ke Artifact Registry
  - name: "gcr.io/cloud-builders/docker"
    args: ["push", "gcr.io/$PROJECT_ID/notes-backend-farros"]

  # Melakukan deploy ke cloud run menggunakan image yang telah di-upload/push
  - name: "gcr.io/cloud-builders/gcloud"
    args:
      [
        "run",
        "deploy",
        "be",
        "--image",
        "gcr.io/$PROJECT_ID/notes-backend-farros",
        "--timeout",
        "3600s",
        "--region",
        "asia-southeast2",
        "--allow-unauthenticated"
      ]
options:
  logging: CLOUD_LOGGING_ONLY
